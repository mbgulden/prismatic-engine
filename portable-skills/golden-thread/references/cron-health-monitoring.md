# Cron Job Health Monitoring\n\nRun this check whenever the user asks about automation health, or as part of the daily Golden Thread sync.\n\n## Quick Health Check\n\n```python\nfrom hermes_tools import cronjob\ncronjob(action='list')\n```\n\n## Job Categories\n\n### User-Visible (delivered to Telegram)\n| Job | Schedule | Delivery |\n|---|---|---|\n| Morning Briefing | Daily 8am MT (14:00 UTC) | Telegram chat |\n| Golden Thread Digest | Daily 9am MT (15:00 UTC) | Telegram chat |\n| Daily Transit Briefing | Daily 1pm UTC | Telegram |\n\n### Script-Based (no agent, stdout delivered)\n| Job | Schedule | Notes |\n|---|---|---|\n| PR Auto-Merger | Every 60m | `pr_auto_merger_and_router.py` |\n| Hermes Journal Snap | Every 60m | `journal_snapshot.py` |\n| Next-Step Backup | Daily 3am | `next-step-backup.sh` (ERROR as of May 31) |\n| Jules Bridge | Every 15m | `jules_session_manager.py` (ERROR as of May 31) |\n\n### Agent-Driven (local delivery)\n| Job | Schedule | Notes |\n|---|---|---|\n| AGY Linear Worker | Every 4hr | Antigravity CLI tasks |\n| AGY Research Reporter | Every 60m | Checks alignment deliverables |\n| Golden Thread Sync | Every 60m | Cross-project state sync |\n| Jules Session Monitor | Every 30m | Jules CLI session tracking |\n| Hermes Journal Recap | Nightly 11:59pm | Daily journal close |\n\n## Error Diagnostics\n\n### Codex-5-5 Jobs (model: chatgpt-5.5)\nThese will ALWAYS fail — `chatgpt-5.5` is a deprecated model name. The jobs `Codex-5-5 Security Review` and `Codex-5-5 Code Quality Review` should be either:\n- Deleted (if the capability isn't needed)\n- Updated to use a current model (e.g., `deepseek-v4-pro`)\n\n### Script-Based Errors\n- Check that the script path exists: `~/.hermes/profiles/orchestrator/scripts/{name}`\n- For `next-step-backup.sh`: verify backup target directories still exist\n- For `jules_session_manager.py`: Jules CLI may not be installed on this host\n\n### Output Visibility\n- Jobs with `deliver: local` save output to `~/.hermes/profiles/orchestrator/cron/output/`\n- Jobs with `deliver: origin` send to the chat that created them (Telegram)\n- The dashboard at port 8080 does NOT surface cron job health — this is a gap\n\n## Dashboard Gap\n\nThe Hermes dashboard (port 8080) is a Node.js login page. It does not show:\n- Cron job status/last run\n- Failed job alerts\n- Output history\n\n**Workaround:** Include a \"Cron Health\" section in the Morning Briefing that lists failures from the last 24 hours.\n\n**Future:** Build a cron-health widget or dedicated endpoint that the dashboard can consume.\n\n## Linear Cycles Integration\n\nLinear Cycles are time-boxed sprints that auto-create, auto-rollover, and track scope. Configuration:\n\n1. Enable under Team Settings → Cycles\n2. Key settings:\n   - Issue rollover (auto-move incomplete work to next cycle)\n   - Auto-add active issues to current cycle\n   - Cooldown period between cycles\n3. Cycle durations: 1-day sprints, 1-week cycles, 1-month initiatives — all supported\n\nFor Michael's 4-phase roadmap:\n- Foundation → 24hr Sprint\n- Production → 1-week Cycle\n- Growth → 1-month Initiative\n- Empire → Backlog (Someday/Maybe)\n\nSee `~/work/hd-reports/linear-boards-v2-roadmap.md` for the full implementation plan.\n